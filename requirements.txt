# Runtime dependencies for the core package and simulation experiments.
numpy>=1.23
scipy>=1.10
scikit-learn>=1.2
networkx>=3.0
joblib>=1.2

# Build-time dependencies for the Cython extensions.
Cython>=3.0

# Plotting dependencies (only needed to reproduce the paper figures).
matplotlib>=3.7
pandas>=2.0
scienceplots>=2.1
